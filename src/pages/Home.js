import React, { Fragment } from "react"
import { ProductCardWithQuery } from "../container/productCardWithQuery"

export const Home = () => {
  return (
    <Fragment>
      <ProductCardWithQuery id={1} />
    </Fragment>
  )
}